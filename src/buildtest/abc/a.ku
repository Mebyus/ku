type s []str

type p *u64

type ZeroCustom void

pub
type String struct {
    ptr: [*]u8
    len: u32
}

fun (&String) len0() -> u32 {
    ret 0;
}

fun ret_tuple1() -> (uint) {
    var k: uint = 0;
    if k != 1 {

    } else if true {

    }

    for 1 == 1 {
        ret 1;
    }

    for true {
        k = 2;
    }

    ret 0;
}

fun ret_tuple2() -> (uint, sint) {
    ret 0, 1;
}

fun use_tuple() -> uint {
    var a: uint;
    a, b := ret_tuple2();
    if a == 0 {
        ret b;
    }
    ret a;
}

// type Variant union {
//     ptr: *void
//     num: uint
// }

fun (&String) klen() -> u32 {
    must(true);

    var a: uint;
    must(a == 0);

    {
        a = 1;
    }
    if g.len0() == 1 {
        a = 2;
    }
    ret g.len;
}

const k := 10;

const a := 1 + 2;

const b := -0x1;

const c := 1 + (3 - 4);

const debug := true;
